class DataSource

  @migrate: (params = {}) ->

  connect: (config) ->

  disconnect: ->

  query: (criteria) ->

  lookup: (id) ->

  insert: (record) ->

  update: (record) ->

  destroy: (id) ->


module.exports = DataSource
